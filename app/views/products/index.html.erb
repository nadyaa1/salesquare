<div class="hidden-sm hidden-xs">
  <div class="container-categories">
    <div class="text-center">
      <li><%= link_to "Women", products_path(category: "women") %></li>
      <li><%= link_to "Shoes", products_path(category: "shoes") %></li>
      <li><%= link_to "Bags", products_path(category: "bags") %></li>
      <li><%= link_to "Men", products_path(category: "men") %></li>
      <li><%= link_to "Kids", products_path(category: "kids") %></li>
      <li><%= link_to "Dresses", products_path(category: "dresses") %></li>
      <li><%= link_to "Coats", products_path(category: "coats") %></li>
    </div>
  </div>

<br>
<div class="stripetrough text-center">
  <h2><span>Find what you are looking for</span></h2>
  <p> A selection of the best products for the best prices</p>
</div>

<div class="container-fluid text-center ">
  <div class="row">
    <div class="products">
      <% @products.each do |p| %>
      <div class="col-xs-12 col-md-4 col-lg-3">
        <div class="products-wrapper">
          <div class="pimage">
           <%= cl_image_tag p.photo.url(:bright_face) %>
         </div>
         <div class="pname">
          <h3><%= p.name %></h3>
        </div>
        <hr/>

        <div class="pcategory">
          € <%= p.price %>
        </div>
        <hr/>
        <div class="pcatlink">
          <%= link_to "show product", product_path(p), class: "showproduct" %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
</div>
