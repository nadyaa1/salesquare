<div class="container-fluid">
  <div class="row">
    <div class="hidden-sm col-md-2">
    </div>
    <div class="col-xs-12 col-md-8">
      <br>
      <div class="stripetrough text-center">
        <h2><span>All your posted products</span></h2>
        <p> Here you can edit and delete</p>
      </div>

      <div class="container-fluid text-center ">
        <div class="row">
          <div class="products">
            <% @products.each do |p| %>
            <div class="col-xs-12 col-md-3 paddingfix">
              <div class="products-wrapper">
                <div class="pimage">
                 <%= cl_image_tag p.photo.url(:bright_face) %>
               </div>
               <div class="pname">
                <h3><%= p.name %></h3>
              </div>


              <div class="pcategory">
                € <%= p.price %>
              </div>
              <hr/>
              <%= link_to product_path(p), class: "showproduct" do %>

              <div class="pcatlink">

          <%= link_to "show product", product_path(p), class: "showproduct" %> |
          <%= link_to "Delete", ([:merchant,p]), method: :delete, class: "deleteproduct" %> |
          <%= link_to "Edit", edit_merchant_product_path(p), class: "editproduct" %>

              </div>
              <% end %>

            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="hidden-sm col-md-2">
  </div>
</div>
</div>

